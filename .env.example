# 🔧 Configuração principal
PORT=3000
NODE_ENV=production

# 🔒 Autenticação JWT
# IMPORTANTE: Gere um segredo forte e único para produção
# Exemplo: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_here_change_this
JWT_EXPIRES_IN=24h

# 🌍 CORS (origem permitida)
# Defina o domínio do seu frontend
CORS_ORIGIN=https://guarda-freios.vercel.app

# 🐘 PostgreSQL Railway
# Obtenha esta URL no dashboard do Railway
# Formato: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://user:password@host:port/database
